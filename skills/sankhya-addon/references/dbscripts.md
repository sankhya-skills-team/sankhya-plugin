# Schema de Banco de Dados (DBScripts)

## Visão Geral

O Add-on Studio permite executar scripts de banco de dados diretamente pelo add-on. Com os `dbscripts` você pode criar tabelas, views, colunas e outros objetos de banco de forma versionada e segura.

Use para cenários não cobertos pelo Auto DDL: criação de views, triggers, migrações de dados, etc.

---

## Estrutura e Organização dos Arquivos

Os scripts devem ficar dentro do diretório `dbscripts` no projeto.

### Versionamento e Nomenclatura

- O nome do arquivo deve começar com `V` (de "versão"), seguido de um número que indica a sequência de execução
- A extensão deve ser `.xml`

**Exemplos:** `V1.xml`, `V2.xml`, `V3.xml`, e assim por diante.

> Seguir essa nomenclatura é fundamental para a atomicidade dos scripts e para minimizar o risco de erros durante o deploy.

### Organizando em Subpastas

Para projetos com muitos scripts, organize em subpastas dentro de `dbscripts`:

```
dbscripts/
    ├── V1_tabelas.xml
    ├── V2_colunas.xml
    └── views/
        ├── V3_vw_projetos.xml
        └── V4_vw_tarefas.xml
```

O sistema percorre as subpastas e executa os scripts respeitando a numeração do prefixo `V`.

---

## Estrutura do Arquivo XML

Cada `.xml` contém um ou mais scripts. A estrutura segue o schema `scripts.xsd`, referenciável para validação na IDE.

```xml
<?xml version="1.0" encoding="ISO-8859-1" ?>
<scripts xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../.gradle/scripts.xsd">

    <sql nomeTabela="SGT_PROJETOS"
         ordem="1"
         executar="SE_NAO_EXISTIR"
         tipoObjeto="TABLE"
         nomeObjeto="SGT_PROJETOS"
         descricao="Cria a tabela de projetos.">
        <mssql>
            CREATE TABLE SGT_PROJETOS (
                ID INT IDENTITY(1,1) PRIMARY KEY,
                NOME VARCHAR(100) NOT NULL,
                DATA_INICIO DATE
            )
        </mssql>
        <oracle>
            CREATE TABLE SGT_PROJETOS (
                ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                NOME VARCHAR2(100) NOT NULL,
                DATA_INICIO DATE
            )
        </oracle>
    </sql>
</scripts>
```

## Atributos da Tag `<sql>`

| Atributo | Obrigatório | Descrição |
|---|---|---|
| `nomeTabela` | Sim | Nome da tabela principal à qual o script se aplica. Usado para referência e logs. |
| `ordem` | Sim | Valor numérico que define a sequência de execução dentro do arquivo. Não pode haver ordens duplicadas no mesmo arquivo. |
| `executar` | Sim | Condição de execução: `SE_NAO_EXISTIR`, `SE_EXISTIR`, `SEMPRE`. |
| `tipoObjeto` | Sim | Tipo do objeto: `TABLE`, `COLUMN`, `VIEW`, `INDEX`, `TRIGGER`, etc. |
| `nomeObjeto` | Sim | Nome único do objeto. Usado para controle de versão e para verificar se o objeto já existe. |
| `descricao` | Não | Breve descrição do que o script faz. Altamente recomendado para documentação. |

> Na IDE, posicione o cursor sobre qualquer atributo ou tag para ver a documentação completa diretamente do arquivo `xsd`.

### Scripts para Múltiplos Bancos

Use `<mssql>` e `<oracle>` para fornecer SQL específico para cada banco. Se o add-on suporta apenas um tipo, inclua apenas a tag correspondente.

---

## Deploy e Verificação

```shell
./gradlew clean deployAddon
```

O log no terminal e no servidor Sankhya exibirá mensagens indicando o sucesso ou a falha de cada script. Após o deploy, acesse o banco e confirme que os objetos foram criados conforme o esperado.

---

## Boas Práticas

- **Use prefixos únicos**: sempre use um prefixo exclusivo para tabelas, views e outros objetos (ex: `SGT_`, `MEUADDON_`). Evita conflitos com tabelas nativas do Sankhya ou de outros add-ons
- **Versionamento imutável**: trate scripts como migrações imutáveis. Uma vez aplicado um `V1.xml`, não o altere — crie `V2.xml`, `V3.xml` para novas modificações
- **Organize em subpastas**: para projetos complexos, use subpastas dentro de `resources/dbscripts`
- **Testes rigorosos**: teste em ambiente de desenvolvimento que espelhe a produção antes de publicar
- **Compatibilidade de banco**: se suporta Oracle e SQL Server, sempre forneça SQL específico para cada um nas tags `<oracle>` e `<mssql>`
- **Mantenha a simplicidade**: scripts pequenos e focados em uma única tarefa facilitam depuração e gerenciamento

## Antipadrões

- **Modificar tabelas nativas diretamente**: nunca adicione, modifique ou remova colunas de tabelas nativas do Sankhya (`TGFPRO`, `TGFCAB`, `TSIUSU`, etc.)
  - Suas alterações podem ser sobrescritas durante uma atualização do sistema
  - Pode causar comportamento inesperado em funcionalidades padrão do ERP
  - Invalida o suporte técnico da Sankhya
  - **Solução correta**: crie uma tabela auxiliar com seu prefixo e relacione-a com a tabela nativa via chave estrangeira. Exemplo: para adicionar "SELO DE GARANTIA" ao produto, crie `SGT_INFOPRODUTO` com `CODPROD` (FK para `TGFPRO.CODPROD`) e `SELOGARANTIA`
- **Usar o prefixo genérico `AD_`**: muito genérico, causa conflitos entre desenvolvedores. Crie um prefixo que identifique unicamente seu projeto
- **Scripts desordenados**: criar uma `VIEW` que consulta uma tabela inexistente irá falhar. Garanta que `ordem` e a numeração dos arquivos sigam uma sequência lógica de dependências
- **Usar ponto e vírgula (`;`)**: evite `;` no final das declarações SQL — a plataforma pode interpretar incorretamente e causar falhas no deploy

## Fonte

https://developer.sankhya.com.br/docs/02_scripts